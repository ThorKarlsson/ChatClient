 connect to a chat server using the CONNECT TO <address|url> command,
 sign in with a user name using USER <name>,
 send messages to the one chat room provided by chat server by typing a message and hitting return,
 receive messages from the one chat room provided by the chat server and display the received messages to the user,
 disconnect from the current chat server using the DISCONNECT command (afterwards it has to be possible to connect again to a chat server),
 print a summary of the supported commands using the command HELP, and
 to leave the chat client using the command QUIT.